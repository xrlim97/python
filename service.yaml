apiVersion: v1
kind: Service
metadata:
 name: simple-service
 labels:
   app: simple-app
spec:
 selector:
   app: simple-app
 type: LoadBalancer
  ports:
  - protocol: TCP
    port: 60000
    targetPort: 50001